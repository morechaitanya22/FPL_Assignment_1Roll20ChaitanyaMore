#include <stdio.h>

int main() {
    int a = 10, b = 20;
    
    // Using the ternary operator to find the maximum value
    int max = (a > b) ? a : b;
    printf("The maximum value is: %d\n", max);
    // Ternary Operator is used for comparing two values.
    return 0;
}
